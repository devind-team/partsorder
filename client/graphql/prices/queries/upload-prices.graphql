mutation UploadPrices($fileUpload: FileUploadInput!) {
  uploadPrices(fileUpload: $fileUpload) {
    headers
    rows {
      rowNo
      success
      productCreate
      data
      __typename
    }
    __typename
  }
}
