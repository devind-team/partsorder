/**
 * Поля для создания цены (Price).
 */
export const priceFields: string[] = ['price', 'duration', 'supplierName', 'country', 'site', 'comment', 'validAt']

/**
 * Поля для создания продукта (Product).
 */
export const productFields: string[] = ['vendorCode', 'name', 'manufacturer']
